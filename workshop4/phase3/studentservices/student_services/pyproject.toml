[project]
name = "student-services-agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "bedrock-agentcore",
    "boto3",
    "httpx",
    "mcp",
    "strands-agents",
]
